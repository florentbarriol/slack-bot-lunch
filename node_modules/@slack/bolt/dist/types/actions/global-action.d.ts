/**
 * A Slack global action wrapped in the standard metadata.
 *
 * This describes the entire JSON-encoded body of a request from Slack global actions.
 */
export interface GlobalAction {
    type: 'global_action';
    callback_id: string;
    trigger_id: string;
    user: {
        id: string;
        name: string;
        team_id?: string;
    };
    channel: {
        id: string;
        name: string;
    };
    team: {
        id: string;
        domain: string;
        enterprise_id?: string;
        enterprise_name?: string;
    };
    token: string;
    action_ts: string;
}
//# sourceMappingURL=global-action.d.ts.map